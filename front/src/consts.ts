export enum keyCodes {
  ArrowUp = 38,
  ArrowDown = 40,
  ArrowLeft = 37,
  ArrowRight = 39,
  Enter = 13,
  Escape = 27,
}

export const menuConsts = {
  defaultColor: 'white',
  chooseColor: 'blue',
  backgraundColor: 'black',
  firstElement: 0,
};

export enum sceneNames {
  Play = 'Play',
  Menu = 'Menu',
}
